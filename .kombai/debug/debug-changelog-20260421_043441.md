## Changes Made

### 1. Updated URL generation logic to favor Localhost
- **Files:**
    - `views/form/view.php`: Modal link generator.
    - `controllers/FormController.php`: `getPublicUrl` method.
    - `controllers/PublishedFormController.php`: `getPublicUrl` method.
- **Change:** Removed logic that prioritizes `APP_URL` or Railway domains. Replaced with `Yii::$app->request->hostInfo` to dynamically use the current active domain (e.g., `http://localhost:8080`).
- **Reason:** User wants all links, including the "Form Published" modal, to point to their local environment since they have disconnected from Railway.
- **Revert:** Restore the `getenv('APP_URL')` and Railway domain check logic.

## Revert Status
- [ ] Change 1 - Localhost Link Logic
