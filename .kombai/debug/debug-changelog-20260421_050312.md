## Changes Made

### 1. Fixed "Class ActiveDatabaseContext Not Found"
- **File:** `controllers/FormController.php`
- **Change:** 
    - Added `use app\components\ActiveDatabaseContext;` to the imports.
    - Used the fully qualified class name `\app\components\ActiveDatabaseContext` in the `getPhysicalDb()` method.
    - Added `use yii\db\Connection;` and ensured the project connection uses this class.
- **Reason:** The controller was trying to use a component class that wasn't correctly imported into its namespace, causing a fatal error during submission to custom tables.
- **Revert:** Remove the imports and revert FQN to just class name.

### 2. Implemented Google Forms-style Success Response
- **File:** `controllers/FormController.php`
- **Change:** Changed `actionSubmit` to always redirect to the `success` page for all users (guests and logged-in) upon successful submission.
- **Reason:** User requested that the submission flow feels like Google Forms, where you see a dedicated success message instead of being redirected back to the form with a alert.
- **Revert:** Restore the conditional redirect based on `Yii::$app->user->isGuest`.

## Revert Status
- [ ] Change 1 - Fix class imports
- [ ] Change 2 - Unified success redirect
